import 'package:flutter/material.dart';

class ErrorWidgetBody extends StatelessWidget {
  const ErrorWidgetBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Error happened'),
    );
  }
}
